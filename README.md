# Домашняя работа 
# Банковские операции - обработка данных

Этот проект предоставляет набор инструментов для обработки банковских операций, включая маскирование номеров карт и счетов, фильтрацию и сортировку операций по различным критериям.

## Функционал

### Маскирование данных

1. **Маскирование номеров карт**
   - Формат: `XXXX XX** **** XXXX`
   - Поддерживает строки и числа
   - Проверяет корректность номера (16 цифр)

2. **Маскирование номеров счетов**
   - Формат: `**XXXX` (последние 4 цифры)
   - Поддерживает строки и числа
   - Проверяет корректность номера (20 цифр)

3. **Функция `mask_account_card`**
   - Автоматически определяет тип данных (карта или счет)
   - Применяет соответствующую маскировку
   - Сохраняет название карты/счета
   
### Генерация номеров карт

1. **Генератор `card_number_generator`**
   - Генерирует номера карт в формате `XXXX XXXX XXXX XXXX`
   - Работает в диапазоне от `0000 0000 0000 0001` до `9999 9999 9999 9999`
   - Принимает начальное и конечное значения диапазона

### Обработка дат

- Преобразование даты из формата ISO (`2024-03-11T02:26:18.671407`) в формат `ДД.ММ.ГГГГ`

### Фильтрация и сортировка операций

1. **Фильтрация по статусу**
   - По умолчанию фильтрует выполненные операции (`EXECUTED`)
   - Поддерживает любой статус (`CANCELED` и др.)

2. **Фильтрация по валюте**
   - Возвращает итератор с транзакциями в заданной валюте
   - Поддерживает все коды валют (`USD`, `EUR`, `RUB` и др.)

3. **Извлечение описаний транзакций**
   - Генератор, который поочередно возвращает описания операций
   - Автоматически пропускает транзакции без описания

4. **Сортировка по дате**
   - По умолчанию сортирует по убыванию (новые сначала)
   - Поддерживает сортировку по возрастанию
   
#### Модуль `decorators.py`
Содержит декоратор `log` для автоматического логирования операций

1**Возможности декоратора**
   - Автоматическое логирование начала/окончания выполнения функций
   - Запись в файл (если указан filename) или вывод в консоль
   - Логирование ошибок 

## Тестирование

Проект включает комплексные тесты для всех функций:

### Тесты маскирования
- Проверка корректного маскирования карт и счетов
- Обработка некорректных данных (короткие/длинные номера, буквы)
- Проверка обработки чисел и строк с пробелами

### Тесты генерации карт
- Проверка генерации номеров в заданном диапазоне
- Тестирование форматирования (группы по 4 цифры)
- Проверка граничных значений (минимальный и максимальный номера)

### Тесты обработки данных
- Фильтрация операций по статусу и валюте
- Сортировка по дате (возрастание/убывание)
- Извлечение описаний транзакций
- Обработка операций без статуса/описания
- Обработка пустых списков

### Тесты виджетов
- Интеграционные тесты функций маскирования карт/счетов
- Тесты преобразования дат
- Проверка обработки некорректных данных

### Тесты для модуля декораторов 
- Логирование в файл
- Логирование в консоль
- Обработка ошибок

## Использование

```python
from src.masks import get_mask_card_number, get_mask_account
from src.widget import mask_account_card, get_date
from src.processing import filter_by_state, sort_by_date
from src.generators import (  
    filter_by_currency,
    transaction_descriptions,
    card_number_generator
)

# Маскирование
print(mask_account_card("Visa Platinum 1234567890123456"))  # Visa Platinum 1234 56** **** 3456
print(mask_account_card("Счет 12345678901234567890"))       # Счет **7890

# Дата
print(get_date("2024-03-11T02:26:18.671407"))              # 11.03.2024

# Генерация номеров карт
for card_number in card_number_generator(1, 5):
    print(card_number)  # 0000 0000 0000 0001 ... 0000 0000 0000 0005

# Обработка операций
operations = [...]
executed_ops = filter_by_state(operations)
usd_transactions = filter_by_currency(operations, "USD")
sorted_ops = sort_by_date(operations, reverse=False)

# Получение описаний
descriptions = transaction_descriptions(operations)
for description in descriptions:
    print(description)
Требования
Python 3.7+

README.md             # Документация